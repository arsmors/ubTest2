# DataSource Configuration for H2 Database (In-memory database for testing)
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password
spring.h2.console.enabled=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# Server Configuration
server.port=8080

# Logging Configuration (Modify as needed)
logging.level.root=INFO

# Security Configuration (Disable CSRF for simplicity, add proper security)
security.basic.enabled=false

# Custom Application Properties (if needed)
# custom.property=value
